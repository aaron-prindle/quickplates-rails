<h1>Listing suggestions</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Lat</th>
    <th>Lng</th>
    <th>Wait</th>
    <th>Votes for</th>
    <th>Votes against</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @suggestions.each do |suggestion| %>
  <tr>
    <td><%= suggestion.name %></td>
    <td><%= suggestion.lat %></td>
    <td><%= suggestion.lng %></td>
    <td><%= suggestion.wait %></td>
    <td><%= suggestion.votes_for %></td>
    <td><%= suggestion.votes_against %></td>
    <td><%= link_to 'Show', suggestion %></td>
    <td><%= link_to 'Edit', edit_suggestion_path(suggestion) %></td>
    <td><%= link_to 'Destroy', suggestion, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Suggestion', new_suggestion_path %>
